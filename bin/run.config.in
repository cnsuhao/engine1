# This file informs executable classes/binaries, avaliable via the Scripts.cmake file,
# the root directory containing the folder structure and data needed to propertly execute

data_dir=@CMAKE_SOURCE_DIR@/data
test_config_dir=@CMAKE_SOURCE_DIR@/test/config
validation_dir=@CMAKE_SOURCE_DIR@/test/validation
verification_dir=@CMAKE_INSTALL_PREFIX@/bin/verification